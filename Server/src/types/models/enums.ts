enum DispatchStatus {
  IDLE = "IDLE",
  LOADED = "LOADED",
  LAUNCHED = "LAUNCHED",
  INTERCEPTED = "INTERCEPTED",
  HIT = "HIT",
}


export default DispatchStatus;


export enum Role {
  ATTACK = "att",
  DEFENSE = 'def',
  ADMIN = 'adm'
}